//event type enum
const ENUM_EVENT_TYPE = {
  virtual: "virtual",
  inPerson: "inPerson",
};

//event privacy enum
const ENUM_EVENT_PRIVACY = {
  public: "public",
  friends: "friends",
  group: "group",
  private: "private",
};

module.exports = {
  ENUM_EVENT_TYPE,
  ENUM_EVENT_PRIVACY,
};
